{
    "buildCommand": "corepack enable && corepack prepare pnpm@9.12.0 --activate && pnpm install && pnpm build",
    "installCommand": "corepack enable && corepack prepare pnpm@9.12.0 --activate && pnpm install",
    "framework": null
  }
  