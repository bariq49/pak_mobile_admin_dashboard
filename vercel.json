{
    "buildCommand": "corepack enable && corepack prepare pnpm@8.15.0 --activate && pnpm install && pnpm build",
    "installCommand": "corepack enable && corepack prepare pnpm@8.15.0 --activate && pnpm install",
    "framework": null
  }
  