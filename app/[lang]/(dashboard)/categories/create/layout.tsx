export const metadata = {
  title: "Create Category",
};

const CategoryCreateLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default CategoryCreateLayout;

